# westarete/tap

This is [West Arete](https://westarete.com/)'s Homebrew tap.

## Usage

```sh
brew tap westarete/tap
brew install westarete/tap/catalog
```

Or install directly without tapping first:

```sh
brew install westarete/tap/catalog
```
